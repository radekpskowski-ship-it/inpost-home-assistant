{
  "config": {
    "step": {
      "user": {
        "title": "InPost - sign in",
        "description": "Enter the phone number from your InPost account (9 digits, no country code). We will send an SMS code.",
        "data": {
          "phone_number": "Phone number"
        }
      },
      "sms": {
        "title": "InPost - SMS code",
        "description": "Enter the 6-digit SMS code sent to +48 {phone}.",
        "data": {
          "sms_code": "SMS code"
        }
      },
      "setup_options": {
        "title": "InPost - options",
        "description": "Pick a phone (from HA Companion app) for distance calculations and configure notifications. All fields are optional - you can skip and configure later via 'Configure'.",
        "data": {
          "device_tracker": "Phone (device_tracker from HA Companion app)",
          "notify_distance_m": "Notify when parcel is closer than (m) [0 = disabled]",
          "notify_service": "Notify service (e.g. notify.mobile_app_<your_phone>)",
          "notify_cooldown_min": "Per-parcel cooldown (min) - do not notify more often than this"
        }
      },
      "reauth_confirm": {
        "title": "InPost - re-authentication",
        "description": "The session expired. We sent a new SMS - enter the code.",
        "data": {
          "sms_code": "SMS code"
        }
      }
    },
    "error": {
      "cannot_send_sms": "Could not send the SMS - check the phone number.",
      "wrong_code": "Invalid or expired SMS code. Try again."
    },
    "abort": {
      "already_configured": "An account with this phone number is already configured.",
      "reauth_successful": "Session refreshed successfully."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "InPost - options",
        "description": "Phone (from HA Companion app) used for distance calculations and notification settings.",
        "data": {
          "device_tracker": "Phone (device_tracker from HA Companion app)",
          "notify_distance_m": "Notify when parcel is closer than (m) [0 = disabled]",
          "notify_service": "Notify service (e.g. notify.mobile_app_<your_phone>)",
          "notify_cooldown_min": "Per-parcel cooldown (min) - do not notify more often than this"
        }
      }
    }
  }
}
